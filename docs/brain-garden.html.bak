<!DOCTYPE html>
<html lang="en" data-theme="light">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Bhaava Brain Garden</title>
  <meta name="description" content="Bhāva Tech Phase 1 mini web app for children: a playful brain training garden with memory, focus, and pattern games." />
  <link rel="preconnect" href="https://api.fontshare.com" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://api.fontshare.com/v2/css?f[]=satoshi@400,500,700&f[]=cabinet-grotesk@500,700,800&display=swap" rel="stylesheet" />
  <style>
    :root, [data-theme="light"] {
      --text-xs: clamp(0.75rem, 0.7rem + 0.25vw, 0.875rem);
      --text-sm: clamp(0.875rem, 0.8rem + 0.35vw, 1rem);
      --text-base: clamp(1rem, 0.95rem + 0.25vw, 1.125rem);
      --text-lg: clamp(1.125rem, 1rem + 0.75vw, 1.5rem);
      --text-xl: clamp(1.5rem, 1.2rem + 1.25vw, 2.25rem);
      --text-2xl: clamp(2rem, 1.2rem + 2.5vw, 3.5rem);
      --space-1: 0.25rem; --space-2: 0.5rem; --space-3: 0.75rem; --space-4: 1rem; --space-5: 1.25rem; --space-6: 1.5rem; --space-8: 2rem; --space-10: 2.5rem; --space-12: 3rem; --space-16: 4rem;
      --color-bg: #f5fbfb;
      --color-surface: #ffffff;
      --color-surface-2: #eef8f7;
      --color-surface-offset: #ddf0ee;
      --color-border: #c7e1df;
      --color-divider: #d7ebe9;
      --color-text: #12363b;
      --color-text-muted: #547378;
      --color-text-faint: #85a2a3;
      --color-text-inverse: #efffff;
      --color-primary: #0a9fa7;
      --color-primary-hover: #0c7e85;
      --color-primary-active: #085c61;
      --color-primary-highlight: #cceeed;
      --color-gold: #f0bc42;
      --color-success: #4f9c62;
      --radius-sm: 0.375rem; --radius-md: 0.75rem; --radius-lg: 1.125rem; --radius-xl: 1.5rem; --radius-full: 9999px;
      --shadow-sm: 0 1px 2px rgb(18 54 59 / 0.08);
      --shadow-md: 0 10px 30px rgb(18 54 59 / 0.10);
      --shadow-lg: 0 18px 50px rgb(18 54 59 / 0.14);
      --transition: 180ms cubic-bezier(0.16, 1, 0.3, 1);
      --font-body: 'Satoshi', 'Inter', sans-serif;
      --font-display: 'Cabinet Grotesk', 'Inter', sans-serif;
    }
    [data-theme="dark"] {
      --color-bg: #0f1e22;
      --color-surface: #14292e;
      --color-surface-2: #183238;
      --color-surface-offset: #1d3f45;
      --color-border: #2c4f54;
      --color-divider: #244248;
      --color-text: #e8f7f6;
      --color-text-muted: #a7c2c1;
      --color-text-faint: #6f8f8e;
      --color-text-inverse: #112529;
      --color-primary: #55c7ce;
      --color-primary-hover: #87dce1;
      --color-primary-active: #bbeef1;
      --color-primary-highlight: #1f363a;
      --color-gold: #f4c95d;
      --color-success: #6fcb86;
      --shadow-sm: 0 1px 2px rgb(0 0 0 / 0.25);
      --shadow-md: 0 10px 30px rgb(0 0 0 / 0.32);
      --shadow-lg: 0 18px 50px rgb(0 0 0 / 0.45);
    }
    *, *::before, *::after { box-sizing: border-box; }
    html { scroll-behavior: smooth; }
    body {
      margin: 0;
      min-height: 100vh;
      font-family: var(--font-body);
      font-size: var(--text-base);
      background:
        radial-gradient(circle at top right, color-mix(in srgb, var(--color-primary) 16%, transparent), transparent 28%),
        radial-gradient(circle at bottom left, color-mix(in srgb, var(--color-gold) 22%, transparent), transparent 22%),
        var(--color-bg);
      color: var(--color-text);
      line-height: 1.55;
    }
    img, svg, canvas { display: block; max-width: 100%; }
    button, input { font: inherit; }
    button { cursor: pointer; }
    a { color: inherit; text-decoration: none; }
    :focus-visible { outline: 3px solid var(--color-gold); outline-offset: 3px; border-radius: var(--radius-sm); }
    .skip-link { position:absolute; left:-999px; top:var(--space-2); background:var(--color-surface); padding:var(--space-3) var(--space-4); z-index:999; }
    .skip-link:focus { left:var(--space-3); }
    .shell { width:min(1150px, calc(100% - 2rem)); margin-inline:auto; }
    .topbar {
      position: sticky; top: 0; z-index: 50;
      backdrop-filter: blur(14px);
      background: color-mix(in srgb, var(--color-bg) 84%, transparent);
      border-bottom: 1px solid color-mix(in srgb, var(--color-text) 10%, transparent);
    }
    .topbar-inner {
      display:flex; align-items:center; justify-content:space-between; gap:var(--space-4);
      min-height:72px;
    }
    .brand { display:flex; align-items:center; gap:var(--space-3); font-weight:700; }
    .brand img { width:52px; height:52px; object-fit:cover; border-radius:50%; box-shadow: var(--shadow-sm); }
    .brand-title { font-family: var(--font-display); font-size: var(--text-lg); }
    .brand-sub { font-size: var(--text-xs); color: var(--color-text-muted); }
    .toolbar { display:flex; align-items:center; gap:var(--space-2); }
    .chip, .theme-toggle, .sound-toggle {
      min-height:44px; border:1px solid color-mix(in srgb, var(--color-text) 12%, transparent);
      background: var(--color-surface); color: var(--color-text); border-radius: var(--radius-full);
      padding: 0 var(--space-4); display:inline-flex; align-items:center; gap:var(--space-2);
      box-shadow: var(--shadow-sm);
    }
    main { padding: var(--space-8) 0 var(--space-12); }
    .hero {
      display:grid; grid-template-columns: 1.2fr 0.8fr; gap:var(--space-8); align-items:center;
      padding: var(--space-8) 0 var(--space-10);
    }
    .hero-card, .panel, .game-card, .badge-card, .completion {
      background: color-mix(in srgb, var(--color-surface) 88%, transparent);
      border: 1px solid color-mix(in srgb, var(--color-text) 10%, transparent);
      border-radius: var(--radius-xl);
      box-shadow: var(--shadow-md);
    }
    .hero-card { padding: clamp(var(--space-6), 4vw, var(--space-10)); }
    .eyebrow {
      display:inline-flex; align-items:center; gap:var(--space-2); margin-bottom:var(--space-4);
      padding: var(--space-2) var(--space-3); border-radius: var(--radius-full);
      background: var(--color-primary-highlight); color: var(--color-primary-active); font-size: var(--text-sm); font-weight:700;
    }
    h1, h2, h3 { margin:0; font-family: var(--font-display); line-height:1.08; }
    h1 { font-size: var(--text-2xl); max-width: 12ch; }
    .hero-card p { color: var(--color-text-muted); max-width: 58ch; }
    .hero-actions { display:flex; flex-wrap:wrap; gap:var(--space-3); margin-top:var(--space-5); }
    .btn {
      min-height:48px; padding: 0 var(--space-5); border:none; border-radius: var(--radius-full);
      font-weight:700; transition: transform var(--transition), background var(--transition), color var(--transition);
    }
    .btn:hover { transform: translateY(-1px); }
    .btn-primary { background: var(--color-primary); color: var(--color-text-inverse); }
    .btn-secondary { background: var(--color-surface-2); color: var(--color-text); border:1px solid color-mix(in srgb, var(--color-text) 10%, transparent); }
    .hero-visual {
      position:relative; min-height: 360px; overflow:hidden; padding:var(--space-6);
      display:flex; align-items:center; justify-content:center;
    }
    .lotus-ring {
      width:min(100%, 340px); aspect-ratio:1; border-radius:50%;
      background:
        radial-gradient(circle at 50% 50%, color-mix(in srgb, var(--color-primary) 16%, transparent) 0 24%, transparent 25%),
        radial-gradient(circle at 50% 50%, color-mix(in srgb, var(--color-gold) 50%, transparent) 0 2%, transparent 3%),
        conic-gradient(from 0deg, color-mix(in srgb, var(--color-primary) 75%, white) 0deg 90deg, color-mix(in srgb, var(--color-gold) 80%, white) 90deg 180deg, color-mix(in srgb, var(--color-success) 70%, white) 180deg 270deg, color-mix(in srgb, var(--color-primary) 75%, white) 270deg 360deg);
      mask: radial-gradient(circle at center, transparent 0 28%, black 29% 74%, transparent 75%);
      animation: spin 14s linear infinite;
      opacity:.9;
    }
    .hero-logo {
      position:absolute; width:160px; height:160px; border-radius:50%; object-fit:cover; background:var(--color-surface);
      padding:var(--space-2); box-shadow: var(--shadow-lg);
    }
    .floating-note {
      position:absolute; padding:var(--space-3) var(--space-4); border-radius: var(--radius-lg); background:var(--color-surface);
      font-size:var(--text-sm); font-weight:700; box-shadow: var(--shadow-sm); border:1px solid color-mix(in srgb, var(--color-text) 10%, transparent);
      animation: float 4.5s ease-in-out infinite;
    }
    .note-1 { top:10%; left:2%; }
    .note-2 { right:4%; top:18%; animation-delay: .8s; }
    .note-3 { bottom:12%; left:10%; animation-delay: 1.6s; }
    .stats { display:grid; grid-template-columns: repeat(3, 1fr); gap:var(--space-4); margin-top:var(--space-6); }
    .badge-card { padding:var(--space-5); }
    .stat-number { font-family: var(--font-display); font-size: var(--text-xl); color: var(--color-primary-active); }
    .stat-label { color: var(--color-text-muted); font-size: var(--text-sm); }
    .section-head { display:flex; align-items:end; justify-content:space-between; gap:var(--space-4); margin:var(--space-6) 0 var(--space-4); }
    .section-head p { color:var(--color-text-muted); max-width: 52ch; margin:0; }
    .games-grid { display:grid; grid-template-columns: repeat(3, 1fr); gap:var(--space-5); }
    .game-card { padding:var(--space-5); display:flex; flex-direction:column; gap:var(--space-4); }
    .game-top { display:flex; justify-content:space-between; gap:var(--space-3); align-items:start; }
    .icon-bubble {
      width:52px; height:52px; border-radius:16px; display:grid; place-items:center; font-size:1.5rem;
      background: color-mix(in srgb, var(--color-primary) 12%, var(--color-surface));
    }
    .game-card p { margin:0; color:var(--color-text-muted); }
    .tag-row { display:flex; flex-wrap:wrap; gap:var(--space-2); }
    .tag { font-size:var(--text-xs); padding:var(--space-1) var(--space-3); border-radius:var(--radius-full); background:var(--color-surface-2); color:var(--color-text-muted); }
    .panel-wrap { display:grid; grid-template-columns: 1fr 320px; gap:var(--space-5); margin-top:var(--space-5); }
    .panel { padding:var(--space-6); }
    .panel[hidden] { display:none; }
    .panel-title { display:flex; justify-content:space-between; align-items:center; gap:var(--space-3); margin-bottom:var(--space-4); }
    .helper { color:var(--color-text-muted); font-size:var(--text-sm); }
    .memory-grid { display:grid; grid-template-columns: repeat(4, minmax(0, 1fr)); gap:var(--space-3); }
    .memory-tile {
      aspect-ratio: 1 / 1; border:none; border-radius: var(--radius-lg); font-size: clamp(1.6rem, 2vw, 2rem);
      background: var(--color-surface-2); color: transparent; box-shadow: inset 0 0 0 1px color-mix(in srgb, var(--color-text) 10%, transparent);
      transition: transform var(--transition), background var(--transition), color var(--transition);
    }
    .memory-tile.flipped, .memory-tile.matched { background: var(--color-primary-highlight); color: var(--color-primary-active); }
    .memory-tile.matched { background: color-mix(in srgb, var(--color-success) 22%, var(--color-surface)); }
    .memory-tile:disabled { cursor: default; }
    .focus-lane {
      position:relative; height:220px; border-radius: var(--radius-xl); background:linear-gradient(180deg, color-mix(in srgb, var(--color-surface-2) 92%, transparent), var(--color-surface));
      overflow:hidden; border:1px solid color-mix(in srgb, var(--color-text) 10%, transparent);
    }
    .focus-player {
      position:absolute; bottom:16px; left:18px; width:82px; height:82px; border-radius:24px;
      display:grid; place-items:center; font-size:2rem; background: var(--color-gold); box-shadow:var(--shadow-sm);
      transition:left 140ms linear;
    }
    .focus-star {
      position:absolute; top:-48px; width:44px; height:44px; border-radius:50%; display:grid; place-items:center; font-size:1.3rem;
      background: color-mix(in srgb, var(--color-primary) 16%, white); animation: fall linear forwards;
    }
    .focus-controls { display:flex; gap:var(--space-3); flex-wrap:wrap; margin-top:var(--space-4); }
    .arrow-btn { min-width:88px; }
    .pattern-stage { display:grid; gap:var(--space-4); }
    .pattern-display, .pattern-options { display:grid; grid-template-columns: repeat(4, minmax(0, 1fr)); gap:var(--space-3); }
    .pattern-cell, .option-btn {
      min-height:72px; border-radius: var(--radius-lg); display:grid; place-items:center; font-size:1.7rem;
      background: var(--color-surface-2); border:1px solid color-mix(in srgb, var(--color-text) 10%, transparent);
    }
    .option-btn.correct { background: color-mix(in srgb, var(--color-success) 22%, var(--color-surface)); }
    .option-btn.wrong { background: color-mix(in srgb, #ff6b6b 20%, var(--color-surface)); }
    .side-stack { display:grid; gap:var(--space-4); }
    .progress-list { display:grid; gap:var(--space-3); }
    .progress-item { padding:var(--space-4); border-radius:var(--radius-lg); background:var(--color-surface-2); }
    .progress-top { display:flex; justify-content:space-between; align-items:center; gap:var(--space-2); margin-bottom:var(--space-2); }
    .bar { width:100%; height:10px; border-radius:999px; background:color-mix(in srgb, var(--color-text) 10%, transparent); overflow:hidden; }
    .bar > span { display:block; height:100%; width:0; background:linear-gradient(90deg, var(--color-primary), var(--color-gold)); border-radius:inherit; transition: width 300ms ease; }
    .tips { padding-left:1rem; margin:0; color:var(--color-text-muted); }
    .tips li + li { margin-top:var(--space-2); }
    .completion { margin-top:var(--space-5); padding:var(--space-5); display:flex; align-items:center; justify-content:space-between; gap:var(--space-4); }
    .completion strong { font-size:var(--text-lg); font-family: var(--font-display); }
    .footer-note { margin-top:var(--space-6); text-align:center; color:var(--color-text-muted); font-size:var(--text-sm); }
    @keyframes spin { from { transform:rotate(0deg); } to { transform:rotate(360deg); } }
    @keyframes float { 0%,100% { transform: translateY(0); } 50% { transform: translateY(-8px); } }
    @keyframes fall { from { transform: translateY(0); } to { transform: translateY(270px); } }
    @media (max-width: 960px) {
      .hero, .panel-wrap, .games-grid { grid-template-columns: 1fr; }
      .stats { grid-template-columns: 1fr; }
      .hero-visual { min-height:300px; }
    }
    @media (max-width: 720px) {
      .topbar-inner, .section-head, .panel-title, .completion { align-items:flex-start; flex-direction:column; }
      .memory-grid { grid-template-columns: repeat(4, 1fr); }
      .pattern-display, .pattern-options { grid-template-columns: repeat(2, 1fr); }
      .shell { width:min(100% - 1rem, 1150px); }
      .hero-card, .panel, .game-card, .badge-card, .completion { border-radius: 1.1rem; }
    }
    @media (prefers-reduced-motion: reduce) {
      *, *::before, *::after { animation-duration: 0.01ms !important; animation-iteration-count: 1 !important; transition-duration: 0.01ms !important; }
    }
  </style>
</head>
<body>
  <a class="skip-link" href="#main-content">Skip to content</a>
  <header class="topbar">
    <div class="shell topbar-inner">
      <div class="brand" aria-label="Bhāva Tech brand">
        <img src="./assets/logo.jpg" alt="Bhāva Tech logo" width="52" height="52" />
        <div>
          <div class="brand-title">Bhāva Tech</div>
          <div class="brand-sub">Phase 1 · Brain Garden mini app</div>
        </div>
      </div>
      <div class="toolbar">
        <div class="chip" aria-live="polite">🌱 Stars: <span id="starCount">0</span></div>
        <button class="sound-toggle" id="soundToggle" aria-pressed="true">🔊 Sound On</button>
        <button class="theme-toggle" id="themeToggle" aria-label="Switch theme">🌙 Theme</button>
      </div>
    </div>
  </header>

  <main id="main-content" class="shell">
    <section class="hero" aria-labelledby="hero-title">
      <div class="hero-card">
        <div class="eyebrow">🧠 Learn · Play · Grow</div>
        <h1 id="hero-title">A joyful brain gym for children in Bhāva Tech Phase 1</h1>
        <p>This small web app turns early cognitive training into short, bright, game-like activities. Children practice memory, focus, and pattern thinking in a calmer way than passive video watching.</p>
        <div class="hero-actions">
          <button class="btn btn-primary" data-open-panel="memory">Start with Memory</button>
          <button class="btn btn-secondary" data-open-panel="focus">Try Focus Catcher</button>
        </div>
        <div class="stats" aria-label="Learning outcomes">
          <article class="badge-card">
            <div class="stat-number" id="gamesFinished">0/3</div>
            <div class="stat-label">Games completed</div>
          </article>
          <article class="badge-card">
            <div class="stat-number" id="bestMemory">0</div>
            <div class="stat-label">Best memory matches</div>
          </article>
          <article class="badge-card">
            <div class="stat-number" id="bestFocus">0</div>
            <div class="stat-label">Best focus score</div>
          </article>
        </div>
      </div>
      <aside class="hero-card hero-visual" aria-label="Playful Bhāva Tech visual">
        <div class="lotus-ring" aria-hidden="true"></div>
        <img class="hero-logo" src="./assets/logo.jpg" alt="Bhāva Tech learning garden emblem" width="160" height="160" />
        <div class="floating-note note-1">🎯 Focus</div>
        <div class="floating-note note-2">🧩 Memory</div>
        <div class="floating-note note-3">🔁 Patterns</div>
      </aside>
    </section>

    <section aria-labelledby="games-title">
      <div class="section-head">
        <div>
          <h2 id="games-title">Three mini games</h2>
          <p>Each activity is short, attractive, and skill-based. A child can move between games freely and collect stars after every success.</p>
        </div>
      </div>
      <div class="games-grid">
        <article class="game-card">
          <div class="game-top">
            <div>
              <h3>Lotus Memory Match</h3>
              <p>Turn over cards and find pairs.</p>
            </div>
            <div class="icon-bubble" aria-hidden="true">🌸</div>
          </div>
          <div class="tag-row"><span class="tag">Working memory</span><span class="tag">Recall</span><span class="tag">Ages 6+</span></div>
          <button class="btn btn-primary" data-open-panel="memory">Play now</button>
        </article>
        <article class="game-card">
          <div class="game-top">
            <div>
              <h3>Star Focus Catcher</h3>
              <p>Move your catcher and grab falling stars.</p>
            </div>
            <div class="icon-bubble" aria-hidden="true">⭐</div>
          </div>
          <div class="tag-row"><span class="tag">Attention</span><span class="tag">Reaction</span><span class="tag">Ages 6+</span></div>
          <button class="btn btn-primary" data-open-panel="focus">Play now</button>
        </article>
        <article class="game-card">
          <div class="game-top">
            <div>
              <h3>Pattern Path</h3>
              <p>See the logic and choose the missing symbol.</p>
            </div>
            <div class="icon-bubble" aria-hidden="true">🔶</div>
          </div>
          <div class="tag-row"><span class="tag">Pattern sense</span><span class="tag">Reasoning</span><span class="tag">Ages 7+</span></div>
          <button class="btn btn-primary" data-open-panel="pattern">Play now</button>
        </article>
      </div>
    </section>

    <section class="panel-wrap" aria-labelledby="play-area-title">
      <div>
        <div class="section-head">
          <div>
            <h2 id="play-area-title">Play area</h2>
            <p>Choose a game card above or use the action buttons below. All progress stays on this page during the session.</p>
          </div>
          <div class="hero-actions">
            <button class="btn btn-secondary" data-open-panel="memory">Memory</button>
            <button class="btn btn-secondary" data-open-panel="focus">Focus</button>
            <button class="btn btn-secondary" data-open-panel="pattern">Pattern</button>
          </div>
        </div>

        <section id="panel-memory" class="panel" aria-labelledby="memory-title">
          <div class="panel-title">
            <div>
              <h3 id="memory-title">Lotus Memory Match</h3>
              <p class="helper">Find all 6 matching pairs. Fewer turns is better.</p>
            </div>
            <button class="btn btn-secondary" id="resetMemory">Reset game</button>
          </div>
          <div class="memory-grid" id="memoryGrid" aria-live="polite"></div>
          <p class="helper" id="memoryStatus">Tap two cards to begin.</p>
        </section>

        <section id="panel-focus" class="panel" aria-labelledby="focus-title" hidden>
          <div class="panel-title">
            <div>
              <h3 id="focus-title">Star Focus Catcher</h3>
              <p class="helper">Catch 10 stars before time ends. Use arrow buttons or keyboard.</p>
            </div>
            <button class="btn btn-secondary" id="startFocus">Start round</button>
          </div>
          <div class="focus-lane" id="focusLane" aria-label="Focus game area">
            <div class="focus-player" id="focusPlayer" aria-hidden="true">🪷</div>
          </div>
          <div class="focus-controls">
            <button class="btn btn-secondary arrow-btn" id="moveLeft">⬅ Left</button>
            <button class="btn btn-secondary arrow-btn" id="moveRight">Right ➡</button>
            <div class="chip">Score: <span id="focusScore">0</span></div>
            <div class="chip">Time: <span id="focusTime">20</span>s</div>
          </div>
          <p class="helper" id="focusStatus">Press start when ready.</p>
        </section>

        <section id="panel-pattern" class="panel" aria-labelledby="pattern-title" hidden>
          <div class="panel-title">
            <div>
              <h3 id="pattern-title">Pattern Path</h3>
              <p class="helper">Look at the sequence and choose the best missing symbol.</p>
            </div>
            <button class="btn btn-secondary" id="nextPattern">Next puzzle</button>
          </div>
          <div class="pattern-stage">
            <div class="pattern-display" id="patternDisplay"></div>
            <div class="pattern-options" id="patternOptions"></div>
            <p class="helper" id="patternStatus">Ready for puzzle 1.</p>
          </div>
        </section>

        <section class="completion" aria-live="polite">
          <div>
            <strong id="gardenMessage">Welcome to the Brain Garden.</strong>
            <p class="helper">Complete all three games to unlock the celebration message.</p>
          </div>
          <button class="btn btn-primary" id="resetAll">Reset all</button>
        </section>
      </div>

      <aside class="side-stack" aria-labelledby="progress-title">
        <section class="panel">
          <div class="panel-title">
            <div>
              <h3 id="progress-title">Progress board</h3>
              <p class="helper">Children see effort and improvement instantly.</p>
            </div>
          </div>
          <div class="progress-list">
            <div class="progress-item">
              <div class="progress-top"><strong>Memory</strong><span id="memoryPercent">0%</span></div>
              <div class="bar"><span id="memoryBar"></span></div>
            </div>
            <div class="progress-item">
              <div class="progress-top"><strong>Focus</strong><span id="focusPercent">0%</span></div>
              <div class="bar"><span id="focusBar"></span></div>
            </div>
            <div class="progress-item">
              <div class="progress-top"><strong>Pattern</strong><span id="patternPercent">0%</span></div>
              <div class="bar"><span id="patternBar"></span></div>
            </div>
          </div>
        </section>
        <section class="panel">
          <div class="panel-title">
            <div>
              <h3>Why this fits Phase 1</h3>
            </div>
          </div>
          <ul class="tips">
            <li>Simple static HTML, CSS, and JavaScript, easy to extend into more games later.</li>
            <li>Short learning loops so children stay active instead of only watching.</li>
            <li>Bhāva Tech branding built in with the attached logo and calm cognitive theme.</li>
            <li>Can be embedded into your main startup website as a first playable module.</li>
          </ul>
        </section>
      </aside>
    </section>

    <p class="footer-note">Bhāva Tech · Cognitive play for children · Phase 1 prototype</p>
  </main>

  <script>
    const state = {
      stars: 0,
      soundOn: true,
      completed: { memory: false, focus: false, pattern: false },
      bestMemory: 0,
      bestFocus: 0,
      bestPattern: 0,
      memory: { matches: 0, turns: 0, first: null, second: null, lock: false },
      focus: { running: false, score: 0, time: 20, playerX: 18, tick: null, spawn: null, stars: [] },
      patternIndex: 0,
      patternScore: 0
    };

    const patterns = [
      { sequence: ['🔺','🔷','🔺','🔷','❓'], answer: '🔺', options: ['🔺','🔶','⭐','🌙'] },
      { sequence: ['🌱','🌱','🌼','🌱','🌱','❓'], answer: '🌼', options: ['🌼','🌸','🌙','⭐'] },
      { sequence: ['1️⃣','2️⃣','3️⃣','1️⃣','2️⃣','❓'], answer: '3️⃣', options: ['2️⃣','3️⃣','4️⃣','1️⃣'] },
      { sequence: ['🟡','🟢','🟡','🟢','❓'], answer: '🟡', options: ['🔵','🟡','🟢','🟣'] }
    ];

    const memoryIcons = ['🪷','🧠','⭐','🎯','🌈','🔶'];
    const memoryGrid = document.getElementById('memoryGrid');
    const memoryStatus = document.getElementById('memoryStatus');
    const focusLane = document.getElementById('focusLane');
    const focusPlayer = document.getElementById('focusPlayer');
    const focusScoreEl = document.getElementById('focusScore');
    const focusTimeEl = document.getElementById('focusTime');
    const focusStatus = document.getElementById('focusStatus');
    const patternDisplay = document.getElementById('patternDisplay');
    const patternOptions = document.getElementById('patternOptions');
    const patternStatus = document.getElementById('patternStatus');

    function beep(freq = 440, duration = 0.12, type = 'sine') {
      if (!state.soundOn) return;
      const ctx = new (window.AudioContext || window.webkitAudioContext)();
      const osc = ctx.createOscillator();
      const gain = ctx.createGain();
      osc.type = type; osc.frequency.value = freq;
      gain.gain.value = 0.04;
      osc.connect(gain); gain.connect(ctx.destination);
      osc.start();
      osc.stop(ctx.currentTime + duration);
      osc.onended = () => ctx.close();
    }

    function shuffle(arr) {
      const copy = [...arr];
      for (let i = copy.length - 1; i > 0; i--) {
        const j = Math.floor(Math.random() * (i + 1));
        [copy[i], copy[j]] = [copy[j], copy[i]];
      }
      return copy;
    }

    function updateBoard() {
      document.getElementById('starCount').textContent = state.stars;
      document.getElementById('bestMemory').textContent = state.bestMemory;
      document.getElementById('bestFocus').textContent = state.bestFocus;
      const finished = Object.values(state.completed).filter(Boolean).length;
      document.getElementById('gamesFinished').textContent = `${finished}/3`;
      const memoryPercent = Math.round((state.memory.matches / 6) * 100);
      const focusPercent = Math.min(100, state.bestFocus * 10);
      const patternPercent = Math.min(100, state.patternScore * 25);
      setProgress('memory', memoryPercent);
      setProgress('focus', focusPercent);
      setProgress('pattern', patternPercent);
      const allDone = Object.values(state.completed).every(Boolean);
      document.getElementById('gardenMessage').textContent = allDone ? 'Wonderful! The whole Brain Garden is blooming.' : 'Welcome to the Brain Garden.';
    }

    function setProgress(key, value) {
      document.getElementById(`${key}Percent`).textContent = `${value}%`;
      document.getElementById(`${key}Bar`).style.width = `${value}%`;
    }

    function reward(game, stars, message) {
      state.stars += stars;
      state.completed[game] = true;
      document.getElementById('gardenMessage').textContent = message;
      updateBoard();
      beep(620, 0.18, 'triangle');
    }

    function initMemory() {
      const deck = shuffle([...memoryIcons, ...memoryIcons]);
      state.memory = { matches: 0, turns: 0, first: null, second: null, lock: false };
      memoryGrid.innerHTML = '';
      deck.forEach((icon, index) => {
        const button = document.createElement('button');
        button.className = 'memory-tile';
        button.type = 'button';
        button.dataset.icon = icon;
        button.dataset.index = index;
        button.setAttribute('aria-label', 'Hidden memory card');
        button.textContent = icon;
        button.addEventListener('click', () => flipCard(button));
        memoryGrid.appendChild(button);
      });
      memoryStatus.textContent = 'Tap two cards to begin.';
      updateBoard();
    }

    function flipCard(button) {
      if (state.memory.lock || button.classList.contains('matched') || button === state.memory.first) return;
      button.classList.add('flipped');
      beep(340, 0.06, 'sine');
      if (!state.memory.first) {
        state.memory.first = button;
        return;
      }
      state.memory.second = button;
      state.memory.turns += 1;
      state.memory.lock = true;
      const match = state.memory.first.dataset.icon === state.memory.second.dataset.icon;
      if (match) {
        state.memory.first.classList.add('matched');
        state.memory.second.classList.add('matched');
        state.memory.first.disabled = true;
        state.memory.second.disabled = true;
        state.memory.matches += 1;
        state.bestMemory = Math.max(state.bestMemory, state.memory.matches);
        memoryStatus.textContent = `Great! ${state.memory.matches} pairs found.`;
        beep(540, 0.12, 'triangle');
        resetMemoryTurn();
        if (state.memory.matches === 6) {
          reward('memory', 3, 'Memory garden complete!');
          memoryStatus.textContent = `You solved all pairs in ${state.memory.turns} turns.`;
        }
      } else {
        memoryStatus.textContent = 'Try again. Watch carefully.';
        setTimeout(() => {
          state.memory.first.classList.remove('flipped');
          state.memory.second.classList.remove('flipped');
          resetMemoryTurn();
        }, 700);
      }
      updateBoard();
    }

    function resetMemoryTurn() {
      state.memory.first = null;
      state.memory.second = null;
      state.memory.lock = false;
    }

    function openPanel(key) {
      ['memory','focus','pattern'].forEach(name => {
        document.getElementById(`panel-${name}`).hidden = name !== key;
      });
    }

    function movePlayer(delta) {
      const laneWidth = focusLane.clientWidth - 100;
      state.focus.playerX = Math.min(Math.max(10, state.focus.playerX + delta), laneWidth);
      focusPlayer.style.left = `${state.focus.playerX}px`;
    }

    function spawnStar() {
      if (!state.focus.running) return;
      const star = document.createElement('div');
      star.className = 'focus-star';
      star.textContent = '⭐';
      const x = Math.random() * (focusLane.clientWidth - 50);
      star.style.left = `${x}px`;
      const duration = 2.2 + Math.random() * 1.2;
      star.style.animationDuration = `${duration}s`;
      focusLane.appendChild(star);
      state.focus.stars.push({ el: star, x, caught: false });
      setTimeout(() => {
        if (star.isConnected) star.remove();
        state.focus.stars = state.focus.stars.filter(item => item.el !== star);
      }, duration * 1000);
    }

    function checkCatch() {
      const playerLeft = state.focus.playerX;
      const playerRight = playerLeft + 82;
      state.focus.stars.forEach(item => {
        const rect = item.el.getBoundingClientRect();
        const laneRect = focusLane.getBoundingClientRect();
        const relativeTop = rect.top - laneRect.top;
        if (!item.caught && relativeTop > 140 && item.x + 22 >= playerLeft && item.x <= playerRight) {
          item.caught = true;
          item.el.remove();
          state.focus.score += 1;
          focusScoreEl.textContent = state.focus.score;
          focusStatus.textContent = 'Nice catch!';
          beep(700, 0.08, 'square');
          if (state.focus.score >= 10) endFocus(true);
        }
      });
      state.focus.stars = state.focus.stars.filter(item => item.el.isConnected && !item.caught);
    }

    function startFocus() {
      clearInterval(state.focus.tick);
      clearInterval(state.focus.spawn);
      state.focus.stars.forEach(item => item.el.remove());
      state.focus = { running: true, score: 0, time: 20, playerX: 18, tick: null, spawn: null, stars: [] };
      focusScoreEl.textContent = '0';
      focusTimeEl.textContent = '20';
      movePlayer(0);
      focusStatus.textContent = 'Catch the stars!';
      state.focus.spawn = setInterval(spawnStar, 850);
      state.focus.tick = setInterval(() => {
        state.focus.time -= 1;
        focusTimeEl.textContent = state.focus.time;
        checkCatch();
        if (state.focus.time <= 0) endFocus(state.focus.score >= 10);
      }, 1000);
      const collisionLoop = setInterval(() => {
        if (!state.focus.running) return clearInterval(collisionLoop);
        checkCatch();
      }, 90);
    }

    function endFocus(win) {
      clearInterval(state.focus.tick);
      clearInterval(state.focus.spawn);
      state.focus.running = false;
      state.bestFocus = Math.max(state.bestFocus, state.focus.score);
      if (win) {
        reward('focus', 4, 'Focus garden complete!');
        focusStatus.textContent = `Excellent! You caught ${state.focus.score} stars.`;
      } else {
        focusStatus.textContent = `Time up. You caught ${state.focus.score} stars. Try once more.`;
        beep(220, 0.16, 'sawtooth');
      }
      updateBoard();
    }

    function renderPattern() {
      const puzzle = patterns[state.patternIndex % patterns.length];
      patternDisplay.innerHTML = '';
      puzzle.sequence.forEach(symbol => {
        const cell = document.createElement('div');
        cell.className = 'pattern-cell';
        cell.textContent = symbol;
        patternDisplay.appendChild(cell);
      });
      patternOptions.innerHTML = '';
      shuffle(puzzle.options).forEach(option => {
        const button = document.createElement('button');
        button.className = 'option-btn';
        button.type = 'button';
        button.textContent = option;
        button.addEventListener('click', () => answerPattern(button, option, puzzle.answer));
        patternOptions.appendChild(button);
      });
      patternStatus.textContent = `Puzzle ${state.patternIndex + 1} of 4.`;
    }

    function answerPattern(button, chosen, answer) {
      const buttons = [...patternOptions.querySelectorAll('button')];
      buttons.forEach(btn => btn.disabled = true);
      if (chosen === answer) {
        button.classList.add('correct');
        state.patternScore += 1;
        patternStatus.textContent = 'Correct! Pattern understood.';
        beep(580, 0.12, 'triangle');
        if (state.patternScore >= 4) reward('pattern', 5, 'Pattern garden complete!');
      } else {
        button.classList.add('wrong');
        patternStatus.textContent = `Good try. The correct answer was ${answer}.`;
        buttons.find(btn => btn.textContent === answer)?.classList.add('correct');
        beep(250, 0.14, 'sawtooth');
      }
      updateBoard();
    }

    document.querySelectorAll('[data-open-panel]').forEach(btn => {
      btn.addEventListener('click', () => openPanel(btn.dataset.openPanel));
    });

    document.getElementById('resetMemory').addEventListener('click', initMemory);
    document.getElementById('startFocus').addEventListener('click', startFocus);
    document.getElementById('moveLeft').addEventListener('click', () => movePlayer(-50));
    document.getElementById('moveRight').addEventListener('click', () => movePlayer(50));
    document.getElementById('nextPattern').addEventListener('click', () => {
      state.patternIndex = (state.patternIndex + 1) % patterns.length;
      renderPattern();
    });
    document.getElementById('resetAll').addEventListener('click', () => {
      state.stars = 0;
      state.completed = { memory: false, focus: false, pattern: false };
      state.bestMemory = 0;
      state.bestFocus = 0;
      state.patternScore = 0;
      state.patternIndex = 0;
      initMemory();
      renderPattern();
      focusStatus.textContent = 'Press start when ready.';
      focusScoreEl.textContent = '0';
      focusTimeEl.textContent = '20';
      updateBoard();
    });

    document.addEventListener('keydown', (event) => {
      if (event.key === 'ArrowLeft') movePlayer(-45);
      if (event.key === 'ArrowRight') movePlayer(45);
    });

    document.getElementById('themeToggle').addEventListener('click', () => {
      const root = document.documentElement;
      root.dataset.theme = root.dataset.theme === 'dark' ? 'light' : 'dark';
    });

    document.getElementById('soundToggle').addEventListener('click', (event) => {
      state.soundOn = !state.soundOn;
      event.currentTarget.textContent = state.soundOn ? '🔊 Sound On' : '🔈 Sound Off';
      event.currentTarget.setAttribute('aria-pressed', String(state.soundOn));
    });

    initMemory();
    renderPattern();
    updateBoard();
  </script>
</body>
</html>
